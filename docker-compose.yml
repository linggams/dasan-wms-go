services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dppiops-api
    ports:
      - "${APP_PORT:-8080}:8080"
    environment:
      - APP_ENV=${APP_ENV:-production}
      - APP_PORT=8080
      - DB_HOST=${DB_HOST:-host.docker.internal}
      - DB_PORT=${DB_PORT:-3306}
      - DB_USER=${DB_USER:-root}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME:-dppimes}
      - JWT_SECRET=${JWT_SECRET}
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS:-*}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - dppiops-network
    # Use this if you need to connect to MySQL on the host
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # Optional: MySQL for development/testing
  # Uncomment if you need a dedicated MySQL container
  # mysql:
  #   image: mysql:8.0
  #   container_name: dppiops-mysql
  #   ports:
  #     - "3307:3306"
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=${DB_PASSWORD:-secret}
  #     - MYSQL_DATABASE=${DB_NAME:-dppimes}
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #     - ./db_dump.sql:/docker-entrypoint-initdb.d/init.sql
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - dppiops-network

networks:
  dppiops-network:
    driver: bridge

# volumes:
#   mysql_data:
